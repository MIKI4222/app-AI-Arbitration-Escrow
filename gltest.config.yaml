networks:
  default: localnet

  localnet:
    url: "http://127.0.0.1:4000/api"
    leader_only: false

  studionet:
    # Pre-configured — accounts auto-generated by GenLayer Studio.

paths:
  contracts: "contract"
  artifacts: "artifacts"
